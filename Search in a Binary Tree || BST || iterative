        while(root!=NULL && root->val!=val)
        {
            if(root->val==val) break;
            if(root->val < val) root = root->right;
            else root = root->left;
            
        }
        return root;
